% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obt-class.R
\name{obt}
\alias{obt}
\title{Start a recipe}
\usage{
obt(folder)
}
\arguments{
\item{folder}{Path to the working folder, as a single string.}
}
\value{
An object of class \code{obt}: the working folder and an empty
operation list.
}
\description{
Creates an empty recipe. Every verb adds one operation to the recipe.
Nothing is downloaded, copied or
written while a recipe is built; \code{obt_commit()} runs the operations in the
order they were added.

\code{folder} is the working folder. Everything the package downloads or writes
later goes under it, in a fixed layout. The folder is recorded here and
read at commit time, so a recipe can be built on a machine where the folder
does not exist yet.
}
\examples{
obt(folder = file.path(tempdir(), "measles"))
}
