% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/commit.R
\name{obt_commit}
\alias{obt_commit}
\title{Run the recipe}
\usage{
obt_commit(obtops, verbose = TRUE)
}
\arguments{
\item{obtops}{An \code{obt} recipe.}

\item{verbose}{Whether each operation is printed as it starts and as it
ends.}
}
\value{
The recipe, invisibly, carrying what the run did: the outcome of
every operation, how long it took, and the files it produced. The paths
are relative to the working folder, so the folder can be moved.
}
\description{
Runs the operations of a recipe, in the order the verbs recorded them. Creates the working
folder, downloads what the recipe asked for, and opens Excel where an
operation needs it.

The run stops at the first operation that fails with a message.
Every run writes one text log under \verb{log/}
in the working folder, whether it finished or stopped.

Running a recipe takes macOS or Windows. An operation whose OutbreakTools entry point has not
shipped yet is turned down before anything is written. Use \code{\link[=obt_describe]{obt_describe()}} to see which
operations of a recipe are waiting and what each one needs.
}
\examples{
\dontrun{
obt(folder = "measles") |>
  obt_designer_add(type = "dev") |>
  obt_designer_languages(dict = "English", form = "ENG") |>
  obt_commit()
}
}
\seealso{
\code{\link[=obt_describe]{obt_describe()}} to read a recipe before running it,
\code{\link[=obt_platform]{obt_platform()}} for what this machine can do.
}
