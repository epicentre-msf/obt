% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/designer-add.R
\name{obt_designer_add}
\alias{obt_designer_add}
\title{Add the OutbreakTools files}
\usage{
obt_designer_add(obtops, type = "main", force = FALSE)
}
\arguments{
\item{obtops}{An \code{obt} recipe.}

\item{type}{The channel: \code{"main"} for the stable release, \code{"dev"} for the
development build.}

\item{force}{Whether the zip is downloaded again when the folder already
holds it.}
}
\value{
The recipe, with the download operation added.
}
\description{
Records the download of the OutbreakTools files. At commit time the zip of
the channel is downloaded into \verb{obt/<channel>/} in the working folder and
unzipped there, and the run keeps the paths of the designer, the empty
setup and the ribbon template it holds.

Every file name is read from the zip, because a name carries the channel
and the version of the build.

A zip already in the folder is used again, replaced With \code{force = TRUE}.

The run refuses a release below version 2.0.0.

Like every verb this one records and answers the recipe. Nothing is
downloaded until \code{obt_commit()} runs.
}
\examples{
obt(folder = file.path(tempdir(), "measles")) |>
  obt_designer_add(type = "dev")
}
