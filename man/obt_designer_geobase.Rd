% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/designer-geobase.R
\name{obt_designer_geobase}
\alias{obt_designer_geobase}
\title{Point the run at a geobase}
\usage{
obt_designer_geobase(obtops, path)
}
\arguments{
\item{obtops}{An \code{obt} recipe.}

\item{path}{The geobase file, as downloaded from the geobase web app.}
}
\value{
The recipe, with the geobase operation added.
}
\description{
Records the geobase the generation run builds with. At commit time the file
is copied into \verb{geo/} in the working folder, and the designer is handed the
copy.

A geobase is optional. A recipe without a geobase will still build.

Like every verb this one records and answers the recipe. Nothing is copied
until \code{\link[=obt_commit]{obt_commit()}} runs.
}
\examples{
geobase <- file.path(tempdir(), "geobase.xlsx")
invisible(file.create(geobase))

obt(folder = file.path(tempdir(), "measles")) |>
  obt_designer_geobase(path = geobase) |>
  obt_designer_generate(name = "measles-2026")
}
\seealso{
\code{\link[=obt_linelist_geobase]{obt_linelist_geobase()}} to read a geobase into a linelist that was
already generated.
}
