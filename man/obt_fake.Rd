% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fake.R
\name{obt_fake}
\alias{obt_fake}
\title{Generate fake datasets for the setup}
\usage{
obt_fake(
  obtops,
  n = 50,
  to = NULL,
  name = FAKE_DEFAULT_NAME,
  overwrite = FALSE,
  seed = NULL,
  id_prefix = "P",
  id_width = NULL,
  range_int = c(0, 100),
  range_num = c(0, 100),
  range_date = c(Sys.Date() - 365, Sys.Date()),
  nchar_text = c(4, 12),
  prop_na = 0.1
)
}
\arguments{
\item{obtops}{An \code{obt} or \code{obt_setup} recipe. Either way the recipe has to
carry a setup, because the setup is what the records are read out of.}

\item{n}{How many records a worksheet holds. A \code{vlist1D} worksheet holds one
whatever this says.}

\item{to}{The folder the file is written into. The default is \verb{export/} in
the working folder. A folder that is missing is created by the run.}

\item{name}{What the file is called, without its extension.}

\item{overwrite}{Whether a file of that name already there may be replaced.}

\item{seed}{The seed the records are drawn under. Give one and two runs of
the same recipe answer the same records. The default draws fresh ones and
leaves the session's own stream as it found it.}

\item{id_prefix}{The lead every generated id carries.}

\item{id_width}{How wide the number of an id is, padded with zeros. The
default is as wide as \code{n} needs.}

\item{range_int}{The range whole numbers are drawn from, where the setup
sets no bound of its own.}

\item{range_num}{The range decimals are drawn from, where the setup sets no
bound of its own.}

\item{range_date}{The range dates are drawn from, where the setup sets no
bound of its own.}

\item{nchar_text}{How long a piece of text is, where the setup sets no bound
of its own.}

\item{prop_na}{How much of every column is left missing, between 0 and 1.}
}
\value{
The recipe, with the generation added.
}
\description{
Generate fake datasets for the setup
}
\examples{
setup <- system.file("extdata", "generic-test-setup.xlsb", package = "obt")

obt_setup(from = setup, folder = file.path(tempdir(), "measles")) |>
  obt_fake(n = 20, seed = 1)
}
\seealso{
\code{\link[=obt_setup]{obt_setup()}} to start a setup recipe,
\code{\link[=obt_designer_geobase]{obt_designer_geobase()}} for the geobase the geo columns are drawn from.
}
