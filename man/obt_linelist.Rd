% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linelist-class.R
\name{obt_linelist}
\alias{obt_linelist}
\title{Start a linelist recipe}
\usage{
obt_linelist(from, folder = NULL, overwrite = FALSE)
}
\arguments{
\item{from}{A linelist workbook, or an \code{obt} recipe to narrow.}

\item{folder}{Path to the working folder, as a single string. Give it with
a linelist workbook. A narrowed recipe carries the folder it came with.}

\item{overwrite}{Whether a linelist of that name already in \verb{linelist/} may
be replaced. With \code{FALSE}, the default, the run stops when one is there,
because that file carries whatever the linelist verbs have written into
it.}
}
\value{
An object of class \code{obt_linelist}.
}
\description{
Creates a recipe that works on one generated linelist, and holds that
linelist and the verbs that touch it. The linelist verbs
\code{\link[=obt_linelist_geobase]{obt_linelist_geobase()}}, \code{\link[=obt_linelist_import]{obt_linelist_import()}} and
\code{\link[=obt_linelist_export]{obt_linelist_export()}} all take a recipe of this class.

\code{from} takes either of two things:
\itemize{
\item \strong{A linelist workbook}, ending in \code{.xlsb}. The recipe records the copy
of that file into \verb{linelist/} in the working folder, and \code{folder} says
where. The file itself is an input and stays as it is: the copy keeps its
name and is what the verbs work on.
\item \strong{An \code{obt} recipe.} It narrows to a linelist one, keeping the working
folder and the linelist operations it already holds. The recipe it came
from is left as it was.
}

A recipe narrowed from an \code{obt} names no linelist of its own. The run reads
the one file under \verb{linelist/} in the working folder, so a linelist built
by an earlier run is picked up. A folder holding more than one is refused,
and naming the file with \code{from} is what settles it.

Like every verb this one records and answers the recipe. Nothing is copied
until \code{\link[=obt_commit]{obt_commit()}} runs.
}
\examples{
linelist <- file.path(tempdir(), "measles-2026.xlsb")
invisible(file.create(linelist))

obt_linelist(from = linelist, folder = file.path(tempdir(), "measles"))

# An `obt` recipe narrows to a linelist one.
obt(folder = file.path(tempdir(), "measles")) |>
  obt_designer_add(type = "dev") |>
  obt_linelist()
}
\seealso{
\code{\link[=obt_linelist_geobase]{obt_linelist_geobase()}}, \code{\link[=obt_linelist_import]{obt_linelist_import()}} and
\code{\link[=obt_linelist_export]{obt_linelist_export()}} for what a linelist recipe can do,
\code{\link[=obt_designer_generate]{obt_designer_generate()}} for building one in the first place.
}
