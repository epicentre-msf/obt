% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linelist-export.R
\name{obt_linelist_export}
\alias{obt_linelist_export}
\title{Write an export out of a generated linelist}
\usage{
obt_linelist_export(
  obtops,
  type = "migration",
  name = NULL,
  to = NULL,
  other = NULL,
  password = NULL
)
}
\arguments{
\item{obtops}{An \code{obt_linelist} recipe.}

\item{type}{The kind of export: \code{"migration"} or \code{"specific"}.}

\item{name}{The name of the export to run, read on a \code{"specific"} export.
The linelist takes \code{"migration"}, \code{"geo"}, \code{"historic"}, \code{"analysis"},
and the number of an export the setup defines.}

\item{to}{The folder the file is written into. The default is \verb{export/} in
the working folder. A folder that is missing is created by the run.}

\item{other}{The linelist the export reads from. The default reads the
linelist the run drives.}

\item{password}{The password \code{other} opens with. Left out where that file
carries none.}
}
\value{
The recipe, with the export added.
}
\description{
Records the export. At commit time the linelist runs it and writes the file
into \verb{export/} in the working folder, or into the folder \code{to} names.

\code{type} says which export runs:
\itemize{
\item \code{"migration"} writes the file \code{\link[=obt_linelist_import]{obt_linelist_import()}} reads back. The
linelist names the file itself, so \code{name} is left out.
\item \code{"specific"} runs one of the exports the setup defines, and \code{name} says
which one.
}

A migration export can read a second linelist instead of the one the run
drives. \code{other} names that file and \code{password} is what it opens with.
Clicking the button raises a prompt for the password, and a scripted run
has nobody to type into that prompt, so the verb carries it. It is hidden
wherever a recipe is printed.

The exports that read a second linelist are \code{"migration"}, \code{"geo"} and
\code{"historic"}. The analysis export and the numbered exports read the
linelist the run drives, and they refuse \code{other}.

Like every verb this one records and answers the recipe. Nothing is written
until \code{\link[=obt_commit]{obt_commit()}} runs.
}
\examples{
# A recipe narrowed from an `obt` reads the linelist the folder holds.
obt_linelist(obt(folder = file.path(tempdir(), "measles"))) |>
  obt_linelist_export(type = "migration")

obt_linelist(obt(folder = file.path(tempdir(), "measles"))) |>
  obt_linelist_export(type = "specific", name = "analysis")
}
\seealso{
\code{\link[=obt_linelist_import]{obt_linelist_import()}} for the verb that reads a migration file
back, \code{\link[=obt_designer_generate]{obt_designer_generate()}} for the linelist this one writes out of.
}
