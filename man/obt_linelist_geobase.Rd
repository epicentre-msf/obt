% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linelist-imports.R
\name{obt_linelist_geobase}
\alias{obt_linelist_geobase}
\title{Read a geobase into a generated linelist}
\usage{
obt_linelist_geobase(obtops, path)
}
\arguments{
\item{obtops}{An \code{obt_linelist} recipe.}

\item{path}{The geobase file, as downloaded from the geobase web app.}
}
\value{
The recipe, with the geobase import added.
}
\description{
Records the geobase import. At commit time the file is copied into \verb{geo/}
in the working folder and read into the linelist the recipe generated.

This is the other half of \code{\link[=obt_designer_geobase]{obt_designer_geobase()}}. That verb hands the geobase
to the designer, so the linelist is built with it. This one reads a geobase
into a linelist that is already built.

The file has to be there when the verb is called, so a path typed wrong is
caught while it still costs nothing.

Like every verb this one records and answers the recipe. Nothing is read
until \code{\link[=obt_commit]{obt_commit()}} runs.
}
\examples{
geobase <- file.path(tempdir(), "geobase.xlsx")
invisible(file.create(geobase))

obt_linelist(obt(folder = file.path(tempdir(), "measles"))) |>
  obt_linelist_geobase(path = geobase)
}
\seealso{
\code{\link[=obt_designer_geobase]{obt_designer_geobase()}} to build a linelist with a geobase,
\code{\link[=obt_designer_generate]{obt_designer_generate()}} for the linelist this one reads into.
}
