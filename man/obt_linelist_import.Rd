% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linelist-imports.R
\name{obt_linelist_import}
\alias{obt_linelist_import}
\title{Read a migrated file into a generated linelist}
\usage{
obt_linelist_import(obtops, from, rule = "append", force = FALSE)
}
\arguments{
\item{obtops}{An \code{obt_linelist} recipe.}

\item{from}{The migration file, ending in \code{.xlsx}. The file has to be there
when the verb is called.}

\item{rule}{What happens to the rows the linelist already holds.
\code{"append"}, the default, puts the import under them. \code{"replace"} clears
them and starts at the first row.}

\item{force}{Whether the import goes past a warning the linelist raised
about the file.}
}
\value{
The recipe, with the import added.
}
\description{
Records the migration import. At commit time the file another linelist
wrote with a migration export is read into the linelist the recipe
generated.

The linelist checks the file before it takes it, and warns on three things:
the file carries no metadata, it records no language, or it was written in
another language. A person clicking the button reads that warning and
decides. A scripted run has nobody to decide, so it stops and reports which
of the three stopped it.

\code{force = TRUE} is what pushes past all three. Use it once you have looked
at the file yourself. \code{\link[=obt_describe]{obt_describe()}} names a recipe that forces, so it is
said out loud before Excel opens.

Like every verb this one records and answers the recipe. Nothing is read
until \code{\link[=obt_commit]{obt_commit()}} runs.
}
\examples{
migrated <- file.path(tempdir(), "cases-from-the-field.xlsx")
invisible(file.create(migrated))

obt_linelist(obt(folder = file.path(tempdir(), "measles"))) |>
  obt_linelist_import(from = migrated, rule = "append")
}
\seealso{
\code{\link[=obt_designer_generate]{obt_designer_generate()}} for the linelist this one reads into.
}
