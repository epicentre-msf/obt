% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obt-print.R
\name{obt_look}
\alias{obt_look}
\alias{obt_operations}
\alias{obt_summary}
\alias{obt_describe}
\alias{print.obt}
\title{Print the operations of a recipe in run order}
\usage{
obt_operations(obtops)

obt_summary(obtops)

obt_describe(obtops)

\method{print}{obt}(x, ...)
}
\arguments{
\item{obtops}{An \code{obt} recipe.}

\item{x}{An \code{obt} recipe.}

\item{...}{Passed nowhere. Present for the \code{print()} method.}
}
\value{
The recipe, unchanged and invisibly.
}
\description{
\code{obt_operations()} prints the numbered list of operations, in the order
\code{obt_commit()} will run them, one line each.

\code{obt_summary()} prints the recipe in a few lines: the working folder, what
it will do, and how much of it is waiting on the workbooks. It is what the
\code{print()} method shows.

\code{obt_describe()} prints everything: the working folder, the values the run
will use, and every operation with its arguments.

All three number the steps, and the two lists print the type of each one
beside it. The number and the type are what \code{\link[=obt_remove]{obt_remove()}} takes.
}
\examples{
recipe <- obt(folder = file.path(tempdir(), "measles")) |>
  obt_designer_generate(name = "measles-2026")

obt_operations(recipe)
obt_summary(recipe)
obt_describe(recipe)
}
