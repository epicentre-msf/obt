% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/platform.R
\name{obt_platform}
\alias{obt_platform}
\alias{obt_staging_clean}
\alias{print.obt_platform}
\title{What this machine can do}
\usage{
obt_platform()

obt_staging_clean()

\method{print}{obt_platform}(x, ...)
}
\arguments{
\item{x}{An \code{obt_platform} record.}

\item{...}{Passed nowhere. Present for the \code{print()} method.}
}
\value{
\code{obt_platform()} answers an object of class \code{obt_platform}, with \code{os},
\code{supported}, \code{staging}, \code{excel_version}, \code{excel_proven} and \code{app}.

\code{obt_staging_clean()} answers the paths it removed, invisibly.
}
\description{
Reads the two things that decide whether a recipe can run here, and answers
them as a record you can print.

\strong{The operating system.} \code{obt_commit()} opens Excel, and that takes macOS
or Windows. On any other system a recipe still builds and prints, and only
the run itself is refused.

\strong{The staging folder, on macOS.} Excel for Mac opens a file inside its own
folder without asking, so the package copies a run in there and moves the
results back out. Reaching that folder takes Full Disk Access for the app
running your R session. Where it cannot be reached the run works in your own
folder and Excel asks about each file it opens.

\strong{The version of Excel, on macOS.} A few versions crash Excel while the
analyses are built. \code{"16.111"} is the one this package was proven on, and
every run records the version it found.

Reading the staging folder writes a small file in it and removes it again,
because a folder macOS refuses to open still answers as present.

\code{obt_staging_clean()} empties the staging folder. A finished run removes its
own folder; this clears what an interrupted run left behind. Run it when no
run is going.
}
\examples{
obt_platform()
}
