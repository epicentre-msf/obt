% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove.R
\name{obt_remove}
\alias{obt_remove}
\alias{obt_pop}
\title{Take a step back out of a recipe}
\usage{
obt_remove(obtops, step)

obt_pop(obtops)
}
\arguments{
\item{obtops}{An \code{obt} recipe, of any of its classes.}

\item{step}{The step to take out: the number \code{\link[=obt_describe]{obt_describe()}} prints beside
it, or the operation type it prints after the label.}
}
\value{
The recipe, with the step and anything recorded beside it removed.
}
\description{
\code{obt_remove()} takes one step out. \code{step} names it, either as the number
\code{\link[=obt_describe]{obt_describe()}} prints beside it or as the type it prints after its label.

\code{obt_pop()} takes the last step out, so a verb called by mistake comes
straight back off.

A step recorded by a verb that recorded several comes out with the rest of
them, and the message says so. \code{\link[=obt_setup_convert]{obt_setup_convert()}} records three steps in
one call, and a recipe holding part of a conversion describes a run that
cannot happen.

A type the recipe holds more than once is refused, because there is no
saying which of them was meant. The message names the numbers, and a number
settles it.

A recipe that has already run is left alone. It carries the record of what
the run did, and a step taken out of that record would say the run did
something it did not.
}
\examples{
recipe <- obt(folder = file.path(tempdir(), "measles")) |>
  obt_designer_add(type = "dev") |>
  obt_designer_languages(form = "ENG") |>
  obt_designer_generate(name = "measles-2026")

# By the number the description prints.
obt_remove(recipe, step = 2)

# By the type, which the description prints after the label.
obt_remove(recipe, step = "designer-languages")

# The last one called comes straight back off.
obt_pop(recipe)
}
\seealso{
\code{\link[=obt_describe]{obt_describe()}} to read the steps and their types,
\code{\link[=obt_operations]{obt_operations()}} for the same list in one line each.
}
