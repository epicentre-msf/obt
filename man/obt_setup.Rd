% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup-class.R
\name{obt_setup}
\alias{obt_setup}
\title{Start a setup recipe}
\usage{
obt_setup(from, folder = NULL, overwrite = FALSE)
}
\arguments{
\item{from}{A setup workbook, or an \code{obt} recipe to narrow.}

\item{folder}{Path to the working folder, as a single string. Give it with
a setup workbook. A narrowed recipe carries the folder it came with.}

\item{overwrite}{Whether a working setup already in the folder may be
replaced. With \code{FALSE}, the default, the run stops when one is there,
because that file carries whatever the setup verbs have written into it.}
}
\value{
An object of class \code{obt_setup}.
}
\description{
Creates a recipe that works on one setup workbook, and holds that setup and
the verbs that touch it. The setup verbs \code{\link[=obt_setup_export]{obt_setup_export()}},
\code{\link[=obt_setup_import]{obt_setup_import()}} and \code{\link[=obt_setup_tags]{obt_setup_tags()}} all take a recipe of this
class.

\code{from} takes either of two things:
\itemize{
\item \strong{A setup workbook}, ending in \code{.xlsb}. The recipe records the copy of
that file into the working folder, and \code{folder} says where. The file
itself is an input and stays as it is: the copy under \verb{setup/source/}
keeps its name, and the copy at \code{setup/setup.xlsb} is what the verbs
work on.
\item \strong{An \code{obt} recipe.} It narrows to a setup one, keeping the working
folder and the setup operations it already holds. The recipe it came
from is left as it was.
}

Like every verb this one records and answers the recipe. Nothing is copied
until \code{\link[=obt_commit]{obt_commit()}} runs.
}
\examples{
setup <- system.file("extdata", "generic-test-setup.xlsb", package = "obt")

obt_setup(from = setup, folder = file.path(tempdir(), "measles"))

# An `obt` recipe narrows to a setup one.
obt(folder = file.path(tempdir(), "measles")) |>
  obt_designer_add(type = "dev") |>
  obt_setup()
}
\seealso{
\code{\link[=obt_setup_export]{obt_setup_export()}}, \code{\link[=obt_setup_import]{obt_setup_import()}} and
\code{\link[=obt_setup_tags]{obt_setup_tags()}} for what a setup recipe can do,
\code{\link[=obt_setup_languages]{obt_setup_languages()}} for the languages a setup file holds.
}
