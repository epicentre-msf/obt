% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup-convert.R
\name{obt_setup_convert}
\alias{obt_setup_convert}
\title{Convert an old setup into the one the designer expects}
\usage{
obt_setup_convert(obtops, from, overwrite = FALSE)
}
\arguments{
\item{obtops}{An \code{obt} recipe.}

\item{from}{The setup to convert, ending in \code{.xlsb} or \code{.xlsx}. The file
has to be there when the verb is called.}

\item{overwrite}{Whether a working setup already in the folder may be
replaced. With \code{FALSE}, the default, the run stops when one is there,
because that file carries whatever the setup verbs have written into it.}
}
\value{
The recipe, with the steps of the conversion added.
}
\description{
Records the conversion of a filled setup, usually built with an older
OutbreakTools, into the empty setup that came with the files
\code{\link[=obt_designer_add]{obt_designer_add()}} downloads. That empty setup is the version the
downloaded designer reads.

The verb records three steps, and the run does them in order:
\enumerate{
\item Copy the file into \verb{setup/source/}. The file the user handed over stays
as it is and is never opened by the run.
\item Run the old setup's own export on that copy, which writes an \code{.xlsx}
into \verb{export/}. A file that already ends in \code{.xlsx} is taken as it
stands, and this step is skipped.
\item Put a fresh copy of the empty setup at \code{setup/setup.xlsb}, and read the
\code{.xlsx} into it. That file is what every later operation works on.
}

The \code{.xlsx} stays in \verb{export/} once the run is over, so a user can open it
and read what came across.

The recipe has to hold \code{\link[=obt_designer_add]{obt_designer_add()}} already, because step 3 fills a
copy of the empty setup the OutbreakTools files carry. The verb says so
when the recipe holds none.

Like every verb this one records and answers the recipe. Nothing is copied
until \code{\link[=obt_commit]{obt_commit()}} runs.
}
\examples{
old <- system.file("extdata", "generic-test-setup.xlsb", package = "obt")

obt(folder = file.path(tempdir(), "measles")) |>
  obt_designer_add(type = "dev") |>
  obt_setup_convert(from = old)
}
\seealso{
\code{\link[=obt_designer_add]{obt_designer_add()}} for the files the conversion fills,
\code{\link[=obt_setup]{obt_setup()}} to work on one setup on its own.
}
