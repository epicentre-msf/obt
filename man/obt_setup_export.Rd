% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup-verbs.R
\name{obt_setup_export}
\alias{obt_setup_export}
\title{Write the setup out as an \code{.xlsx} file}
\usage{
obt_setup_export(obtops, to = NULL)
}
\arguments{
\item{obtops}{An \code{obt_setup} recipe.}

\item{to}{The folder the file is written into. The default is \verb{export/} in
the working folder. A folder that is missing is created by the run.}
}
\value{
The recipe, with the export operation added.
}
\description{
Records the export. At commit time the sheets of the working setup are
written into an \code{.xlsx} file, which can be read from R or edited outside
Excel.

The layout of that file is the setup's own: it is what the setup writes and
what \code{\link[=obt_setup_import]{obt_setup_import()}} reads back.

Like every verb this one records and answers the recipe. Nothing is written
until \code{\link[=obt_commit]{obt_commit()}} runs.
}
\examples{
setup <- system.file("extdata", "generic-test-setup.xlsb", package = "obt")

obt_setup(from = setup, folder = file.path(tempdir(), "measles")) |>
  obt_setup_export()
}
\seealso{
\code{\link[=obt_setup]{obt_setup()}} to start a setup recipe, \code{\link[=obt_setup_import]{obt_setup_import()}} to
read a file back in.
}
