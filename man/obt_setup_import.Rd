% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup-verbs.R
\name{obt_setup_import}
\alias{obt_setup_import}
\title{Read an \code{.xlsx} file into the setup}
\usage{
obt_setup_import(obtops, from)
}
\arguments{
\item{obtops}{An \code{obt_setup} recipe.}

\item{from}{The \code{.xlsx} file to read.}
}
\value{
The recipe, with the import operation added.
}
\description{
Records the import. At commit time the content of \code{from} is written into
the working setup. The file keeps the layout the setup's own export writes,
so a file \code{\link[=obt_setup_export]{obt_setup_export()}} produced is a file this verb reads.

The file has to be there when the verb is called, so a path typed wrong is
caught while it still costs nothing.

Like every verb this one records and answers the recipe. Nothing is read
until \code{\link[=obt_commit]{obt_commit()}} runs.
}
\examples{
setup <- system.file("extdata", "generic-test-setup.xlsb", package = "obt")

content <- file.path(tempdir(), "setup-content.xlsx")
invisible(file.create(content))

obt_setup(from = setup, folder = file.path(tempdir(), "measles")) |>
  obt_setup_import(from = content)
}
\seealso{
\code{\link[=obt_setup]{obt_setup()}} to start a setup recipe, \code{\link[=obt_setup_export]{obt_setup_export()}} to
write one out.
}
