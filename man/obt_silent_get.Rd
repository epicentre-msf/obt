% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/silent.R
\name{obt_silent_get}
\alias{obt_silent_get}
\title{Read the open-time switch of a workbook}
\usage{
obt_silent_get(path, password = NULL)
}
\arguments{
\item{path}{The workbook to read. A setup, a designer, or a generated
linelist.}

\item{password}{The password the workbook opens with. A linelist built
with one takes it here. Left out, the default, the workbook is opened
with none.}
}
\value{
\code{"Yes"} or \code{"No"}.
}
\description{
Answers what a workbook does with the message boxes it shows while it is
opening: \code{"Yes"} when it shows none, \code{"No"} when it behaves as it always
has.

\code{"No"} is the answer for a workbook that carries no switch at all, which is
every workbook built before the switch existed.

This opens Excel, so it takes macOS or Windows.
}
\examples{
\dontrun{
obt_silent_get("~/measles/linelist/measles-2026.xlsb")
obt_silent_get("~/measles/linelist/measles-2026.xlsb", password = "pw")
}
}
\seealso{
\code{\link[=obt_silent_set]{obt_silent_set()}} to write it.
}
