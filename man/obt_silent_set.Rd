% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/silent.R
\name{obt_silent_set}
\alias{obt_silent_set}
\title{Write the open-time switch of a workbook}
\usage{
obt_silent_set(path, value = SILENT_ON, password = NULL)
}
\arguments{
\item{path}{The workbook to write. A setup, a designer, or a generated
linelist.}

\item{value}{\code{"Yes"} or \code{"No"}.}

\item{password}{The password the workbook opens with. A linelist built
with one takes it here. Left out, the default, the workbook is opened
with none.}
}
\value{
The value the workbook holds once the run is over, invisibly.
}
\description{
Tells a workbook whether to show the message boxes it shows while it is
opening. \code{"Yes"} quiets them; \code{"No"} leaves the workbook as it is, and is
what every workbook carries to begin with.
}
\details{
The value is written once, on the copy the package works with, and it
stays. Nothing flips it around an operation: an operation run from R arms
its own silence inside Excel, for the length of its own call.

The value is read by the OutbreakTools code inside the workbook, so it
quiets a workbook whose version knows the switch. On an older one the value
is written and nothing reads it, and the boxes come up as before.

This opens Excel, so it takes macOS or Windows.
}
\examples{
\dontrun{
obt_silent_set("~/measles/linelist/measles-2026.xlsb", "Yes")
obt_silent_set("~/measles/linelist/measles-2026.xlsb", "Yes", password = "pw")
}
}
\seealso{
\code{\link[=obt_silent_get]{obt_silent_get()}} to read it.
}
